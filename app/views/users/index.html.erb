<% provide(:title, 'Usuarios') %>
<h1>Usuarios de SocialMusic</h1>
<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate @users, :class => 'pag_bottom' %>

