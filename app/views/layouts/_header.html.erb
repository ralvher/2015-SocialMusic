<nav class="navbar navbar-fixed-top navbar-inverse col-sm-12 col-xs-12 col-md-12 col-lg-12">
  <div class="container col-sm-12 col-xs-12 col-md-12 col-lg-12">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "SocialMusic", root_path, id: "logo", class:"navbar-brand" %>
    </div>

    <div id="navbar" class="navbar-collapse collapse">
      <% if logged_in? %>
      
        <ul class="nav navbar-nav navbar-right" >

          <li><%= link_to "Usuarios <i class='fa fa-users my-fa'> </i>".html_safe , users_path %></li> 
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_user.name %> <b class="caret"></b>
            </a>            
            <ul class="dropdown-menu">
              <li><%= link_to "<i class='fa fa-user my-fa'> </i> Perfil ".html_safe, current_user %></li>
              <li><%= link_to "<i class='fa fa-cog my-fa'> </i>  Configuración ".html_safe, edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
              <%= link_to "<i class='fa fa-power-off my-fa'> </i> Salir ".html_safe, logout_path, method: "delete" %>
            </li>
            </ul>
            
          </li>
        </ul>
        <form class="navbar-form navbar-right" action="/buscar" >
              <input role="search" type="text" id="buscar" name="buscar" class="form-control" placeholder="Buscar Usuario...">
        </form>

      <% else %>
        <%= form_for(:session, url: login_path, :html => {:class => "navbar-form navbar-right my-form-in"} )do |f| %>
          <div class="form-group">
            <%= f.label :email,  :class => "ftext"  %> <br>
            <%= f.email_field :email, class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.label :Contraseña, :class => "ftext" %> 
            <%= link_to "¿Olvidaste contraseña?", new_password_reset_path, :class => "forgot" %> <br>
            <%= f.password_field :password, class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.label :recuerdame, :class => "myc" %>
            <br>
            <%= f.check_box :remember_me %>
          </div>
          <div class="form-group">
          <br>
            <%= f.submit "Entrar", class: "entrar btn btn-default" %>
          </div>
        <% end %>

      <% end %>
    </div>
  </div>
</nav>
