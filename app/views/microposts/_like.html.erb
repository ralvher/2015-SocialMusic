<%= form_for(@post.active_relationlikes.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :liker_id, @user.id %><%= hidden_field_tag :post_id, @post.id %></div>
  <%= f.submit "Like", class: "btn btn-primary" %>
<% end %>